# Lean deps unused on Vercel after Modal cutover (kept for reference / local experiments).
# Production API runs on Modal (web/modal_app.py) with xgboost in the Modal image.
fastapi>=0.115.0
pydantic>=2.0.0
